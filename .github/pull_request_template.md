## What this changes

<!-- One or two sentences. What is different for the person using Verso? -->

## Why

<!-- The reason, and what you considered instead if the choice was not
     obvious. This is the part that saves the next person from undoing it. -->

## Checks

- [ ] `npm run check:all` is green
- [ ] A test that would have caught this (for a fix) or that covers it (for a
      feature)
- [ ] Screenshots in both themes, if anything visual changed
- [ ] Nothing rewrites bytes the reader did not type; nothing degrades silently
